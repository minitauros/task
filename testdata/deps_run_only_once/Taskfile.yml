version: '3'

tasks:
  a:
    cmds:
      - rm -f a.txt
      - echo "a" >> a.txt
    deps:
      - b
      - c

  b:
    cmds:
      - rm -f b.txt
      - echo "b" >> b.txt
    deps:
      - c
      - d

  c:
    cmds:
      - rm -f c.txt
      - echo "c" >> c.txt
    deps:
      - d

  d:
    cmds:
      - rm -f d.txt
      - echo "d" >> d.txt
